{
  "name": "piranhahr-superapp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env --import ./loader.mjs index.js",
    "dev": "node --env-file=.env --import ./loader.mjs --watch index.js"
  }
}